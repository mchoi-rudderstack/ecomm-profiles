# Project name
name: ecomm_loyal_customers
# Project's yaml schema version
schema_version: 40
# WH Connection Profile to use.
connection: adhoc_wh
# Whether to allow inputs having no timestamps,
# if true, data without timestamps are included when running models
include_untimed: true
# Model folders to use.
model_folders:
  - models
# Entities in this project and their ids.
entities:
  - name: user
    id_stitcher: models/ecomm_loyal_customers_stitcher
    id_types:
      - user_id
      - anonymous_id
      - email
      - cart_token
      - flx_id
  - name: session
    id_stitcher: models/ecomm_session_stitcher
    id_types:
      - session_main_id
      - session_id
id_types:
  - name: user_id
  - name: cart_token
  - name: anonymous_id
    filters:
      - type: exclude
        value: ""
      - type: exclude
        value: "unknown"
      - type: exclude
        value: "NaN"
  - name: email
    filters:
      - type: include
        regex: ".+@.+"
      - type: exclude
        value: "test@company.com"
  - name: flx_id
  - name: session_id
  - name: session_main_id
